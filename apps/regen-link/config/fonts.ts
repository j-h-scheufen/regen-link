import { Figtree, Inter, Sora } from 'next/font/google';

export const fontInter = Inter({
  subsets: ['latin'],
  variable: '--font-inter',
  display: 'swap',
});

export const fontSora = Sora({
  subsets: ['latin'],
  variable: '--font-sora',
  display: 'swap',
});

export const fontFigtree = Figtree({
  subsets: ['latin'],
  variable: '--font-figtree',
  display: 'swap',
});
