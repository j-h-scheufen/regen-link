import { AddEntityForm } from '@/components/forms';

const AddEntityPage = () => {
  return (
    <section className="h-screen w-screen flex flex-col justify-center">
      <AddEntityForm />
    </section>
  );
};

export default AddEntityPage;
